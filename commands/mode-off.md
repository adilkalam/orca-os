---
description: "DEPRECATED — Use /mode -off"
allowed-tools: [Bash, exit_plan_mode]
argument-hint: "optional note"
---

# /mode-off (DEPRECATED)

Use `/mode -off` to disable verification checks for trusted sprints.

```bash
bash scripts/verification-mode.sh off
```

Re-enable later with `/mode-tweak` or `/mode-strict`.
